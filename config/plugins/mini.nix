{
  plugins.mini = {
    enable = true;

    modules = {
      icons = { };
      # comment = { mappings = { comment = "<leader>/"; }; };
      comment = { };
      pairs = { };
      # ai = { };
      surround = { };
    };
  };
}
