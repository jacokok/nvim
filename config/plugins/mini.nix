{
  plugins.mini = {
    enable = true;

    modules = {
      icons = { };
      comment = { mappings = { comment = "<leader>/"; }; };
      pairs = { };
      # ai = { };
      surround = { };
    };
  };
}
